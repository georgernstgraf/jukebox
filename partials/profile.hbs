<div class="profile">
    {{#if session.username}}
        <button class="btn btn-secondary"
                type="button"
                hx-post="{{config.mountpoint}}/logout"
                hx-target="body">log out</button>
    {{else}}
        <p class="m-0">You are not logged in.</p>
    {{/if}}
</div>